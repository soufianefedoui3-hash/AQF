DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-a-long-random-secret-in-production"
ADMIN_EMAIL="admin@aqf.ma"
ADMIN_PASSWORD="Admin@AQF2026"

# Production on Hostinger: leave unset to auto-use prisma/production.db (writable path)
# DATABASE_URL="file:./dev.db"

# Emergency bypass if bootstrap blocks boot (temporary only)
# SKIP_DB_BOOTSTRAP="true"

# Optional: set to "true" once to force-reset the admin password from ADMIN_PASSWORD
# ADMIN_FORCE_RESET="false"

# Optional: secret for POST /api/setup/fix-admin and /api/setup/bootstrap
# SETUP_SECRET="your-long-random-setup-secret"
# FIX_ADMIN_SECRET="your-long-random-setup-secret"
